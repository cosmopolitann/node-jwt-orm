const expressJwt = require("express-jwt");

/*
const secretKey="123456"
const jwtAuth = expressJwt({secret: secretKey}).unless({path: ["/users/login","/hello","/login/hello"]});


//export default jwtAuth;

module.exports = jwtAuth;

*/

